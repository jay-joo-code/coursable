
export default async () => {
  // const requirement = await Requirement.findById('6059c41f1678013b3ddb6dff')
  // console.log('requirement :>> ', requirement)
  // const plan = await Plan.findOne({ shortId: 'TcneQpSd' })
  // console.log('plan.semesters[1].length :>> ', plan.semesters[1].length)
}
